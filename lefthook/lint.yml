pre-commit:
  parallel: true
  commands:
    actionlint:
      glob: ".github/workflows/*.{yaml,yml}"
      run: task lint:actionlint
      fail_text: "actionlint found issues"

    yamllint:
      glob: "**/*.{yaml,yml}"
      run: task lint:yamllint
      fail_text: "yamllint found issues"

    tool-json:
      glob: "tool.json"
      run: task lint:tool-json
      fail_text: "invalid tool.json"
